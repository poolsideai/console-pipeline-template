name: Index on Push

on:
  push:

jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - uses: poolsideai/bridge-sdk/action@main
        with:
          repository_id: ${{ secrets.POOLSIDE_REPOSITORY_ID }}
          token: ${{ secrets.POOLSIDE_API_TOKEN }}
          api_base_url: ${{ secrets.POOLSIDE_API_URL }}
